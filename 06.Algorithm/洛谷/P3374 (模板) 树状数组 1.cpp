#include <bits/stdc++.h>

/* Least Significant Bit of x having a value of 1. */
#define LSB(x) ((x) & -(x))

using namespace std;

class FenwickTree {
private:
    vector<int> numbers;

public:
    FenwickTree() {

    }
};

int main() {
    int n, query;
    scanf("%d%d", &n, &query);


    return 0;
}